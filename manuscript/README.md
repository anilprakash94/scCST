# Manuscript reproducibility

This directory is reserved for simulation code, benchmarking comparisons, figure-generation notebooks used for the scCST manuscript.

These files are not required to run scCST on a new dataset. For normal usage, see `src/sccst/`, `scripts/`.
